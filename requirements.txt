# Core dependencies for the compliance engine, schema, and report generator.
pydantic>=2.0
PyYAML>=6.0
reportlab>=4.0

# Testing
pytest>=7.0

# Optional: only needed if you call the real Claude API path in
# ai_fallback.classify / report_generator.explain (both default to a
# real Anthropic call but accept an injectable api_call_fn / explain_fn
# for testing without this installed).
anthropic>=0.34
