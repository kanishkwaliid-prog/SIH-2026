#config-version=FGVM64-7.2.4-FW-build1396-230206:opmode=1:vdom=0:user=admin
config system global
    set admin-telnet enable
    set admintimeout 30
    set hostname "FGT-DEMO"
    set pre-login-banner enable
    set timezone 04
end
config system interface
    edit "port1"
        set vdom "root"
        set ip 192.168.1.99 255.255.255.0
        set allowaccess ping https ssh http telnet
        set alias "WAN"
    next
    edit "port2"
        set vdom "root"
        set ip 10.0.0.1 255.255.255.0
        set allowaccess ping https ssh
        set alias "LAN"
    next
end
config system admin
    edit "admin"
        set accprofile "super_admin"
        set password ENC SH2sZ5uV3xYq0kFhJm1p9LwT4aBcDeFgHiJkLmNoPqRsTuVwXyZ
    next
end
config system dns
    set primary 8.8.8.8
    set secondary 8.8.4.4
end
config log syslogd setting
    set status enable
    set server "10.0.0.5"
end
config system snmp community
    edit 1
        set name "public"
    next
end
config firewall policy
    edit 1
        set name "LAN-to-WAN"
        set srcintf "port2"
        set dstintf "port1"
        set action accept
    next
end